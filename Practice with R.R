1 == 1 # "==" means "is equal to"; only use this when comparing integers (whole numbers)
1 != 1 #"!=" means "is not equal to"
1:5 # R will spit out a set of values 1-5
.mass # R will hide this variable if there is a period in front of it
  # what are hidden variables for?

## Challenge 2
